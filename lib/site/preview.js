// The cookie that carries a private preview token from /preview-site/<token>
// to every page of the unpublished site (see app/preview-site/[token]/route.js).
export const PREVIEW_COOKIE = 'mach_preview'
